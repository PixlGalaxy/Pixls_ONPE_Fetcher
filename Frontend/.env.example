# API Backend Configuration
# For development, leave as '/api' to use Vite's proxy to localhost:8000
# For production (Docker), set to your backend API URL
VITE_API_BASE=/api

# Backend target URL (only used during development with Vite proxy)
# This is the actual backend server address that the dev proxy will forward requests to
VITE_API_TARGET=http://localhost:8000
